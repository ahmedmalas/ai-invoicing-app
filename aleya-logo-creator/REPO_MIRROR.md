# Repository mirror note

This directory is the deployable source for **ALEYA Logo Creator**.

It is intended to live in `github.com/ahmedmalas/ALEYA-LOGO-CREATOR`.
This cloud agent could not push to that repository (HTTP 403 for cursor[bot]),
so the complete standalone product is also versioned here inside
`ahmedmalas/ai-invoicing-app` under `aleya-logo-creator/` until write access
to the dedicated repo is granted.

Shared backend: Supabase project `aleya-logo-creator` + Edge Function `logo-platform`.
