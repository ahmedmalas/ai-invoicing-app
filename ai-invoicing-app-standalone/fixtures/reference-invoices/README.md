# Reference invoices

`Cart_N_Tip_107.pdf` is the visual source of truth for the Quantum Hire layout.
A copy also lives under `tests/fixtures/reference-invoices/` for automated tests.

Both paths are listed in `.vercelignore` and must never be served by production.
